{% include 'dashboard/components/headerDashboard.twig' %}
<div class="page">
    {% include'dashboard/components/menuDashboard.twig' %}
    <main>
        <div class="breadcrumb grey lighten-3">
            <h6>Cautare student</h6>
        </div>
        <div class="card custom-card">
            <form action="{{ path_for('getGradesUserDashboard') }}" method="get">
                <table class="striped">
                    <tr>
                        <td class="right-align">Numar matricol</td>
                        <td><div class="input-field"><input type="text" name="id" placeholder="Introduceti numarul matricol"></div></td>
                        <td><button type="submit" class="btn green waves-light waves-effect">Cauta</button></td>
                    </tr>
                </table>
            </form>
        </div>
    </main>
</div>
{% include 'dashboard/components/footerDashboard.twig' %}